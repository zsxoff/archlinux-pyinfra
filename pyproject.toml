[project]
name = "archlinux-pyinfra"
version = "0.1.0"
description = "Arch Linux with pyinfra"
readme = "README.md"
requires-python = ">=3.12"
dependencies = ["pyinfra==3.5.1"]

[dependency-groups]
dev = ["mypy[faster-cache]==1.18.2", "pyright==1.1.407", "ruff==0.14.2"]

[tool.ruff]
line-length = 88
